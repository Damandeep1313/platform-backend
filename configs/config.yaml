server:
  port: 8080

database:
  host: localhost
  port: 5432
  user: postgres
  password: yourpassword
  name: platformdb
  sslmode: disable

redis:
  addr: localhost:6379
  password: ""
  db: 0

jwt:
  secret: your_jwt_secret_key
